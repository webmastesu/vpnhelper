import 'package:flutter/material.dart';

class CatShowPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cat Show'),
      ),
      body: Center(
        child: Text('Welcome to the Cat Show!'),
      ),
    );
  }
}
